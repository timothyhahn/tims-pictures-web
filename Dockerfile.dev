FROM node:20-alpine

WORKDIR /app

# Copy all files first so prepare script can run
COPY . .

# Install dependencies
RUN npm install

# Expose the port
EXPOSE 5173

# Start the development server
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]
